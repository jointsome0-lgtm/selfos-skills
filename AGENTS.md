# selfos-skills — Portable Agent Skills Catalog

The installable source of truth is `skills/<name>/`, following the open Agent Skills format. Each skill's `metadata.selfos.version` is canonical, and every change in that installable tree requires a strict semantic-version increase. Bundled dependency copies are generated from each composed skill's `BUNDLE.json` manifest, self-identify through a `GENERATED.md` marker, and retain their source skill's version, while the containing composed skill is bumped when its regenerated tree changes. Both host adapter versions are generated by separately summing the catalog's major, minor, and patch components on top of the committed `VERSION_BASE.json` offset, which absorbs removed skills' components so the aggregate stays monotonic; substantive aggregate-adapter changes require packaging bumps for every skill. Installers and native plugin systems discover those folders directly; this file is only a generated catalog and a fallback for agents that can read repository instructions but do not yet support skills.

When working inside this repository, read a matching skill's `SKILL.md` in full and resolve every relative path from that skill folder. Rows marked **explicit only** must not auto-activate from a topical match. This repository is public: use invented examples only and never commit personal data, credentials, private paths, or local agent state.

<!-- Generated by scripts/build_index.py; do not edit by hand. -->
| Name | Version | When to use | Invocation | Bundled references | Path |
| --- | --- | --- | --- | --- | --- |
| `codebase-design` | `0.1.3` | Use when the user wants to design or improve a module's interface, find deepening opportunities, decide where a seam goes, or make code more testable or AI-navigable, or when another skill needs the shared deep-module design vocabulary — module, interface, depth, seam, adapter, leverage, locality, the deletion test. | automatic or explicit | — | [skills/codebase-design/SKILL.md](skills/codebase-design/SKILL.md) |
| `compose` | `0.2.2` | Use when delegating coding, review, diagnosis, or research work to Codex, a Claude subagent, or another model-backed agent, or when writing or updating a prompt or agent instructions for one — produces a lean, outcome-first delegate prompt with a dated model and effort choice. | automatic or explicit | — | [skills/compose/SKILL.md](skills/compose/SKILL.md) |
| `delegate-pr-loop-query` | `0.3.2` | Use when the current agent must stop operating an open pull request that still needs Codex review rounds, fixes, and an exact-HEAD clean verdict — produces one ready-to-run query so a fresh agent can continue the PR to merge. | automatic or explicit | `compose`, `handoff` | [skills/delegate-pr-loop-query/SKILL.md](skills/delegate-pr-loop-query/SKILL.md) |
| `grilling` | `0.2.2` | Use when the owner wants to stress-test a plan, decision, or idea, uses a 'grill' trigger phrase, or when a domain workflow skill needs the shared decision-interview loop — relentless question rounds with a recommended answer each, and no action before owner confirmation. | automatic or explicit | — | [skills/grilling/SKILL.md](skills/grilling/SKILL.md) |
| `handoff` | `0.1.2` | Use when work must continue in a fresh session, directly or as a primitive inside a composite workflow — creates one compact, privacy-safe Markdown handoff in the operating system's temporary directory so the next agent resumes from durable artifacts. | automatic or explicit | — | [skills/handoff/SKILL.md](skills/handoff/SKILL.md) |
| `improve-codebase-architecture` | `0.2.4` | Use when the user asks for an architecture review of a codebase, wants deepening or refactoring candidates surfaced visually in an HTML report, or wants to decide which architectural friction to tackle first — then grills through whichever candidate the owner picks. | automatic or explicit | `codebase-design`, `grilling`, `sdd-conventions` | [skills/improve-codebase-architecture/SKILL.md](skills/improve-codebase-architecture/SKILL.md) |
| `prototype` | `0.1.1` | Use when the user wants to sanity-check whether a state model or logic feels right, or explore what a UI should look like before committing to a build — a throwaway prototype on a worktree branch answers one design question, and only the validated decision lands. | automatic or explicit | — | [skills/prototype/SKILL.md](skills/prototype/SKILL.md) |
| `research` | `0.1.1` | Use when the user wants a topic researched, docs or API facts gathered, or reading legwork delegated so the main session keeps working — a background subagent investigates against high-trust primary sources and saves one Markdown file citing each claim's source. | automatic or explicit | — | [skills/research/SKILL.md](skills/research/SKILL.md) |
| `sdd-conventions` | `1.0.1` | Use when adopting, updating, or validating selfos-style SDD section conventions or Decision Log entries in any agent or CI environment — packages the shared grammar plus standalone sync and lint scripts for vendoring. | automatic or explicit | — | [skills/sdd-conventions/SKILL.md](skills/sdd-conventions/SKILL.md) |
| `slice` | `0.2.4` | Use when the user asks to slice an implementation-ready SDD section, phase, or approved parent issue into tickets, or to turn an approved SDD scope into implementation issues — dependency-ordered vertical GitHub issues, published only after owner confirmation. | automatic or explicit | `grilling`, `sdd-conventions` | [skills/slice/SKILL.md](skills/slice/SKILL.md) |
| `wait-what` | `0.1.1` | Use when the owner explicitly says they lost the thread, asks "wait, what?", or asks for the last message explained again simply — re-pitches the previous assistant message in Simplified Technical English using the repository's established domain terms. | **explicit only** | — | [skills/wait-what/SKILL.md](skills/wait-what/SKILL.md) |
| `watch` | `0.5.1` | Use when the user asks to babysit a PR, watch or wait for the Codex review, or run the push-review-fix loop — iterates fixes within a caller-owned round budget and sees a clean verdict through CI checks and merge. | automatic or explicit | — | [skills/watch/SKILL.md](skills/watch/SKILL.md) |
| `wayfinder` | `0.2.4` | Use when the user asks to chart or work a wayfinder map, or when an effort is too big and foggy for a single planning session — a shared GitHub map of decision tickets, worked one at a time into the SDD Decision Log until an implementation-ready scope is clear. | automatic or explicit | `grilling`, `sdd-conventions` | [skills/wayfinder/SKILL.md](skills/wayfinder/SKILL.md) |

Git worktrees: create them only in `.worktrees/<name>` inside the repo (globally gitignored via `~/.config/git/ignore`), never as sibling directories. Any work that will open a PR branches and builds in such a worktree, never in the primary checkout — the primary checkout stays on a clean `main` so parallel sessions don't fight for its index. Trivial read-only work and single-file doc edits on a clean main need no worktree. Remove the worktree and delete its local branch once its PR merges.

Installs do not self-update: a merged skill change reaches only the repository, so a release ends with refreshing every local install channel on each machine — the marketplace cache before the host plugin (a stale cache can reinstall an outdated snapshot), plus any flat per-skill copies.
